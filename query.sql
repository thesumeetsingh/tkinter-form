CREATE DATABASE pythonForm;

USE pythonForm;

CREATE TABLE userdetail (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255),
    branch VARCHAR(255),
    gender VARCHAR(255),
    contact VARCHAR(255),
    email VARCHAR(255),
    address VARCHAR(255)
);
